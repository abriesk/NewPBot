# Tooling configuration only. The application is not packaged or installed --
# it runs from /app in the image, with `prepend_sys_path = .` for Alembic.

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # isort
    "UP",        # pyupgrade
    "B",         # bugbear
    "C4",        # comprehensions
    "DTZ",       # flake8-datetimez -- bans naive datetimes (hard rule 4)
    "ASYNC",     # flake8-async
    "S",         # bandit
    "RUF",
]
ignore = [
    "S101",      # assert: the test suite is built on it
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S105", "S106"]      # hardcoded test credentials are fine
# The Armenian full stop is the point of these golden tests: §11.5 exists to
# prove non-ASCII punctuation reaches a client unmangled.
"tests/channels/test_markdown.py" = ["RUF001"]
# Same reason: a config file crosses installations with real Russian and
# Armenian content inside it, and the tests say so in the content.
"tests/core/test_config_io.py" = ["RUF001"]
# The Armenian day heading is asserted letter for letter. Several of its
# letters are confusable with Latin ones, which is exactly what the golden
# test is there to catch -- the fix is not to write it in Latin.
"tests/channels/test_dates.py" = ["RUF001"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"datetime.datetime.utcnow".msg = "Naive UTC is banned. Use datetime.now(UTC)."

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
strict = true
warn_unused_ignores = true
disallow_untyped_decorators = false
exclude = ["alembic/versions/"]

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
# datetime.utcnow() is deprecated in 3.12 and banned outright (hard rule 4).
# Surfacing deprecations as errors keeps that from creeping back in quietly.
filterwarnings = ["error::DeprecationWarning"]

[tool.ruff.lint.isort]
# `alembic/` next to `app/` makes the dependency look first-party once a
# migration under it is linted, which would reshuffle alembic/env.py's imports.
# It is a third-party package that happens to own a directory here.
known-third-party = ["alembic"]
